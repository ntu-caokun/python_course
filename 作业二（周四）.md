## 上海房价预测 Codabench 线上实验
背景：上海二手房数据建模、回归预测与线上评测。

本次作业以“上海房价预测”为主题。教师已在 Codabench 平台建立课程私有竞赛，学生需要基于公开训练集完成数据分析、特征工程和模型训练，并将可自动运行的预测代码提交到平台。平台会使用隐藏测试集运行学生代码，计算得分并生成排行榜。

比赛链接：[Shanghai House Price Prediction](https://www.codabench.org/competitions/16702/?secret_key=5c896b29-600c-4c03-8cbd-48cef079b6a4)

## 一、编程任务
1. 进入 Codabench 课程竞赛页面，下载 Starting Kit。Starting Kit 中包含：

```latex
README.md
train.csv
predict.py
```

2. 读取 `train.csv`，理解字段含义并完成必要的数据清洗、缺失值处理、类别特征编码、文本或地理位置特征处理等步骤。

公开训练集 `train.csv` 共 60000 条样本、13 个字段。隐藏测试集与训练集具有相同的特征字段，但不包含 `house_price`，隐藏标签只用于平台评分。

| 类型 | 字段 |
| --- | --- |
| 样本标识 | `Id` |
| 位置与小区信息 | `district`, `subdistrict`, `community` |
| 房源文本信息 | `title` |
| 户型与面积 | `bedrooms`, `livingrooms`, `area_sqm` |
| 楼层信息 | `floor_region`, `total_floor` |
| 朝向与建成年份 | `orientation`, `build_year` |
| 预测目标 | `house_price` |


说明：**URL、图片字段和单价字段已被移除，以避免信息泄露并降低噪声。**

3. 围绕 `house_price` 训练一个回归模型。`house_price` 表示房屋总价，单位为“万元”。模型可以使用线性回归、随机森林、梯度提升、神经网络或其他你掌握的方法，但最终提交必须能在平台环境中运行。
4. 修改或重写 `predict.py`，使其能够按照平台规定的命令行参数读取训练集、隐藏测试集，并生成预测文件 `predictions.csv`。
5. 将 `predict.py` 以及模型推理所需的附加文件一起打包为 zip，提交到 Codabench 平台参与线上评测和排行榜冲榜。

## 二、作业目标
1. 能独立完成一个机器学习回归任务的完整流程：数据读取、探索分析、预处理、训练、验证、保存模型和平台推理。
2. 理解本地验证成绩与线上隐藏测试成绩之间可能存在的差异，能分析过拟合、数据分布和模型泛化问题。
3. 至少成功提交一次 Codabench，并保留排行榜或提交详情截图。鼓励多次改进模型，在排行榜中取得更高名次。
4. 排行榜主指标为 `Score`，`Score` 越高，排名越高。平台同时显示 `RMSE` 和 `MAE`。

每天最多可提交 2 次，请合理安排实验，避免只依赖排行榜反复试错。作业目标不是简单复制参考 `predict.py`，而是通过特征工程、模型选择、参数调优和误差分析提升线上成绩。


## 三、作业要求
1. 在 Codabench 平台提交 zip 文件。zip 内必须包含：

```latex
predict.py
```

2. 如果 `predict.py` 需要加载模型文件，也必须一并放入 zip，例如：

```latex
predict.py
model.pkl
```



3. 代码中必须包含清晰注释，说明主要函数、变量含义、特征处理流程和模型预测逻辑。
4. Codabench 会自动调用学生提交的程序：

```bash
python3 predict.py --train train.csv --test test.csv --output predictions.csv
```

参数含义：

+ `--train`：公开训练集路径，可用于在线训练、统计特征或读取训练分布。
+ `--test`：隐藏测试集路径，只有特征，没有 `house_price`。
+ `--output`：预测结果输出路径，程序必须在该路径生成 CSV 文件。
5. 输出文件必须严格包含两列：

```plain
Id,house_price
53686,750.0
40585,700.0
```

注意：

+ `Id` 必须来自测试集，不能修改、遗漏或新增。
+ `house_price` 必须是有限数值，不能出现空值、NaN 或无穷大。
+ 不要在输出文件中加入多余列。
6. Canvas 最终提交压缩包命名为：

```latex
周四_组号_姓名_作业2.zip
```

建议压缩包内包含：关键代码文件、最终提交到 Codabench 的 zip 文件、排行榜或提交详情截图，以及必要的运行说明。以小组为单位提交作业，注明组号以及提交者的姓名。

请在截止时间06月14日前完成 Codabench 提交。

## 四、运行环境与依赖建议
Codabench 运行环境为 Python 3.10，Docker image 为 `codalab/codalab-legacy:gpu310`。建议本地也使用 Python 3.10 并根据 requirements.txt 安装依赖进行开发和测试.  
`requirements.txt` 包含以下类型的常用库：

+ 数据处理：`pandas`、`numpy`、`scipy`
+ 机器学习：`scikit-learn`
+ 深度学习：`tensorflow`、`torch`
+ 可视化：`matplotlib`、`seaborn`
+ 进度显示：`tqdm`

Codabench 当前运行环境已经测试可用：

```latex
Cython==3.0.10
numpy==1.26.4
matplotlib==3.9.0
seaborn==0.13.2
scipy==1.14.0
scikit-learn==1.5.0
pandas==2.2.2
PyYAML==6.0.1
imutils==0.5.4
opencv-python==4.10.0.84
torch==2.3.1
tensorflow==2.16.1
tqdm==4.66.4
psutil==6.0.0
h5py==3.11.0
jupyter==1.0.0
```

Codabench 当前运行环境不支持：

```latex
xgboost
lightgbm
catboost
```

平台运行时不保证有 GPU。即使 `torch` 和 `tensorflow` 是 CUDA 版本，也请按 CPU 可运行的方式编写提交代码。

如果使用 `joblib` 或 `pickle` 保存 scikit-learn 模型，请尽量保证训练环境和平台环境中的 scikit-learn 版本接近，否则可能出现模型加载失败。

> 如有问题请及时与任课教师或助教沟通。